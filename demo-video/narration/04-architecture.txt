The architecture is deliberately simple. The Go server owns the simulation, advances it at sixty ticks per second, persists world state, and broadcasts updates over WebSockets. Browsers handle input and rendering. That makes the client thin, the state consistent, and the experience accessible from a modern browser.
