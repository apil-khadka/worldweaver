name: Deploy to Dokploy

on:
  push:
    branches: [main]

jobs:
  deploy-backend:
    name: Deploy Backend
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Dokploy Backend Webhook
        run: |
          curl -X POST "${{ secrets.DOKPLOY_BACKEND }}" \
            -H "Content-Type: application/json" \
            -d '{"ref": "refs/heads/main", "branch": "main"}' \
            --fail --silent --show-error

  deploy-frontend:
    name: Deploy Frontend
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Dokploy Frontend Webhook
        run: |
          curl -X POST "${{ secrets.DOKPLOY_FRONTEND }}" \
            -H "Content-Type: application/json" \
            -d '{"ref": "refs/heads/main", "branch": "main"}' \
            --fail --silent --show-error
